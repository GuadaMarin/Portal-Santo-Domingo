import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PortalWeb_SantoDomingo',
  webDir: 'dist'
};

export default config;
